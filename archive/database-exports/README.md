# Database Exports

Raw database export files are intentionally kept out of Git because they can contain API tokens, provider credentials, or other local configuration secrets.

Keep the original `.sql` files only in the local WPS-synced folder. If a sanitized export is needed later, create a redacted copy and review it before committing.

